# Analist-Agent — requirements.txt
# Not: Versiyonlar makul üst sınırlar olmadan bırakıldı; gerektiğinde kilitleyebilirsin.
# Çekirdek
pydantic>=2.6
pyyaml>=6.0.1

# Tool‑calling & Orkestrasyon
langchain-core>=0.3
langgraph>=0.2

# UI (opsiyonel ama mevcut ui_streamlit.py için gerekli)
streamlit>=1.36

# İstemci/HTTP (LLM servis wrapper’ı için olası gereksinim)
httpx>=0.27

# DB Sürücüleri (ihtiyaca göre kurulabilir)
# SQLite, Python ile gömülü gelir.
psycopg[binary]>=3.1       # PostgreSQL (opsiyonel)
# duckdb>=1.0.0            # Alternatif/opsiyonel

# SQL yardımcıları (geçmişte validator/EXPLAIN için işine yarayabilir)
sqlglot>=23.0

# Geliştirici deneyimi (opsiyonel)
# rich>=13.7
# tqdm>=4.66